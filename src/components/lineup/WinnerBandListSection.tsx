import { SectionContainer } from '../shared/SectionContainer';

export const WinnerBandListSection = () => {
  return (
    <SectionContainer id="vyherci-kapel" bgColor="blue">
      <h2 className="text-h2-reversed text-beige">Výherci soutěže kapel</h2>
    </SectionContainer>
  );
};
